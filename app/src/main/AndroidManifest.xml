<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dhcc.datacage">

    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS" />
    <!-- 这个权限用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 这个权限用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="gBkoDF8XaVSUo6ZAxjpWExK92WYLLTYt" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 主界面-->
        <activity android:name=".activity.MainActivity"/>
        <!-- 工作台模块 -->
        <!-- 音视频会议-->
        <activity android:name=".activity.workbench.VedioConference_Activity" android:windowSoftInputMode="adjustPan"/>
        <!-- 待办已办综合查询-->
        <activity android:name=".activity.workbench.IntegrateQuery_Activity" android:windowSoftInputMode="adjustPan"/>

        <!-- 执法模块 -->
        <!-- 调查材料取证-->
        <activity android:name=".activity.law.Investigation_Activity" android:windowSoftInputMode="adjustResize|adjustPan"/>
        <!-- 案件审理材料填报-->
        <activity android:name=".activity.law.CaseHearing_Activity" android:windowSoftInputMode="adjustPan"/>
        <!-- 案件登记-->
        <activity android:name=".activity.law.CaseRegister_Activity" android:windowSoftInputMode="adjustResize|adjustPan"/>
        <!-- 案件审核-->
        <activity android:name=".activity.law.CaseAudit_Activity" android:windowSoftInputMode="adjustPan"/>
        <!-- 结案归档提交审批-->
        <activity android:name=".activity.law.CaseFiling_Activity" android:windowSoftInputMode="adjustPan"/>
        <!-- 执法人员位置信息-->
        <activity android:name=".activity.law.LawLocation_Activity"/>
        <!-- 疑情疑点信息登记上报查询 -->
        <activity android:name=".activity.law.Doubt_Activity"/>
        <!-- 监督检查信息登记上报查询 -->
        <activity android:name=".activity.law.Supervise_Activity"/>

        <!-- 协同模块 -->
        <!-- 团队互助-->
        <activity android:name=".activity.synergy.TeamWork_Activity"/>

        <!-- 设置模块 -->
        <!-- 修改密码-->
        <activity android:name=".activity.setting.UpdatePwd_Activity" android:windowSoftInputMode="adjustPan"/>



    </application>

</manifest>